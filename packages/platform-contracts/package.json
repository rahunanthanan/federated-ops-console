{
  "name": "@platform/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript contracts: region, auth, remotes, events, error context. Zero runtime dependencies - types + tiny guards only.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
